[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"

# Netlify will automatically detect Next.js and use the correct settings
# The publish directory should be set to ".next" in the Netlify dashboard

